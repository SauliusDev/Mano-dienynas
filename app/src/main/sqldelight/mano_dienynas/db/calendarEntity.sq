CREATE TABLE calendarEntity (
    id INTEGER NOT NULL PRIMARY KEY,
    title TEXT,
    start TEXT,
    url TEXT,
    type TEXT,
    allDay INTEGER
);

getCalendarById:
SELECT *
FROM calendarEntity
WHERE id = :id;

getAllCalendar:
SELECT *
FROM calendarEntity;

insertCalendar:
INSERT OR REPLACE
INTO calendarEntity
VALUES(?, ?, ?, ?, ?, ?);

deleteCalendarById:
DELETE FROM calendarEntity
WHERE id = :id;

deleteAllCalendar:
DELETE FROM calendarEntity;