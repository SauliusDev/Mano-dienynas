CREATE TABLE settingsEntity (
    keepSignedIn INTEGER,
    selectedSchoolInfo TEXT
);

getSettings:
SELECT *
FROM settingsEntity;

insertSettings:
INSERT OR REPLACE
INTO settingsEntity
VALUES(?, ?);

deleteSettings:
DELETE FROM settingsEntity;
