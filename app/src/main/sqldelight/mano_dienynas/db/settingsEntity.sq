CREATE TABLE settingsEntity (
    keepSignedIn INTEGER
);

getSettings:
SELECT *
FROM settingsEntity;

insertSettings:
INSERT OR REPLACE
INTO settingsEntity
VALUES(?);

deleteSettings:
DELETE FROM settingsEntity;
